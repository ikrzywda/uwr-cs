#include <stdio.h>
#include <math.h>

unsigned long long int convert_to_decimal(unsigned p, char *n) {
    char c;
    unsigned int power;
    unsigned long long int output = 0;
    while ((c = *(n++)) != '\0') {
         
    }
    return 0;
}

char *convert_bases(unsigned p, unsigned q, char *n) {
    return 0;
}


int main() {

}
